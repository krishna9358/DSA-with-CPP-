#include <bits/stdc++.h>
using namespace std;

int main(){
    // for (declaration ; condition to stop;  process to do){
        //code to run;
        // scope of i is in this only
    // }

    for (int i = 0 ; i< 10; i++){
        cout << "Hii " << i <<endl;
    }

    // in reverse 
    for (int j=10; j >0; j--){
        cout << j << endl;
    }

    //nested for loop are loosp inside the loops

    return 0;
}